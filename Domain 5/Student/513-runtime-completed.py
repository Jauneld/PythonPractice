"""runtime error: Happens when one tries to run code. They happen when one tries
to do with an app what cannot be done as the app's code is being run. it can
stem from LOGIC ERRORS. They cause various different types of error codes. 
"""
print(__doc__)
plant_types = ['angiosperms','ferns','mosses']
print(plant_types[0])
print(plant_types[3])
