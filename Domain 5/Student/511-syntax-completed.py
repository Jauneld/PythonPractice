#syntax errors: punctuation and spelling errors in writing code. Code can't run

# build a function to multiply a score and a multiplier
def calculate_score(score, multiplier): #added colon
    print('score upcoming')
    return(score * multiplier)#incorrect indent
    
print(calculate_score(3000,2))#missing parentheses
print(calculate_score(3200,1.5))
